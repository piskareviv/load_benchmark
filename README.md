# How to run


```bash
python3 run.py <proc-model> # outputs to tmp/ folder
```

Make sure `g++`, `clang++` and `perf` commands are available in terminal.

Also make sure `perf stat -d` does work and can be run without `sudo`.
